/*#include <stdio.h>
#include <stdlib.h>

int factorial(int n)
{
	printf("Factorial(%d)\n", n);
	if (n <= 1) return(1);
	else return (n * factorial(n - 1));
}

int main(void)
{
	int num;
	scanf("%d", &num);
	num = factorial(num);
	printf("%d", num);

	return 0;
}*/