/*#include <stdio.h>
#include <stdlib.h>

double slow_power(double x, int n)
{
	int i;
	double result = 1.0;

	for (i = 0; i < n; i++)
		result *= x;
	return(result);
}

int main(void)
{
	double base = 0.0;
	int power = 0;
	scanf("%lf %d", &base, &power);
	double outcome = Power(base ,power);
	printf("%.2lf", outcome);
}*/